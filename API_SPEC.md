# TYP_ENGINEER API 仕様書

このドキュメントは、タイピングアプリ「TYP_ENGINEER」のAPIが提供する機能をまとめたものです。  
技術的な知識がない方にも理解しやすいよう、各機能を平易な言葉で説明しています。

---

## 目次

1. [概要](#概要)
2. [単語関連](#単語関連)
   - [タイピング練習用の単語を取得する](#1-タイピング練習用の単語を取得する)
   - [単語の種類一覧を取得する](#2-単語の種類一覧を取得する)
   - [品詞の一覧を取得する](#3-品詞の一覧を取得する)
   - [全単語の詳細情報を取得する](#4-全単語の詳細情報を取得する)
3. [ユーザー関連](#ユーザー関連)
   - [ユーザー登録](#5-ユーザー登録)
   - [ログイン](#6-ログイン)
   - [ログアウト](#7-ログアウト)
4. [タイピング結果関連](#タイピング結果関連)
   - [タイピング練習の結果を保存する](#8-タイピング練習の結果を保存する)
   - [全単語ごとのタイピング回数を取得する](#9-全単語ごとのタイピング回数を取得する)
   - [マイページのデータを取得する](#10-マイページのデータを取得する)

---

## 概要

| 項目 | 内容 |
|------|------|
| ベースURL | `http://localhost:8888` |
| データ形式 | JSON |

---

## 単語関連

### 1. タイピング練習用の単語を取得する

**URL：** `GET /api/typWord`

**何ができるか：**  
タイピング練習に使う単語を、指定した条件に合わせて取得します。  
たとえば「動詞だけ」「名詞を20個」「aから始まる単語だけ」のように絞り込んで取得することができます。

**絞り込み条件（任意）：**

| 条件名 | 説明 | 指定できる値の例 |
|--------|------|------------------|
| type | 単語の難易度・種類 | `basic`（基本）、`advance`（応用）、`beginner`（初級）、`abbreviation`（略語）、`initials`（頭字語）、`ALL`（全て） |
| parts_of_speech | 品詞の種類 | `名詞`、`動詞`、`形容詞` など |
| alphabet | 単語の先頭文字 | `a` ～ `z`、`ALL`（全て） |
| quantity | 取得する単語の件数 | 数字（例：`10`） |

**返ってくる情報：**  
条件に合致した単語の一覧（単語・品詞・説明）

---

### 2. 単語の種類一覧を取得する

**URL：** `GET /api/types`

**何ができるか：**  
タイピング練習で選択できる単語の「種類（カテゴリ）」の一覧を取得します。  
アプリの絞り込みメニューなどに使われます。

**条件：** なし

**返ってくる情報：**  
単語の種類の一覧（例：基本、応用、初級 など）

---

### 3. 品詞の一覧を取得する

**URL：** `GET /api/partsofspeech`

**何ができるか：**  
タイピング練習で選択できる「品詞（名詞・動詞・形容詞など）」の一覧を取得します。  
アプリの絞り込みメニューなどに使われます。

**条件：** なし

**返ってくる情報：**  
品詞の一覧（例：名詞、動詞、形容詞 など）

---

### 4. 全単語の詳細情報を取得する

**URL：** `GET /api/wordDetail`

**何ができるか：**  
アプリに登録されている全ての単語の詳細情報を取得します。  
単語の意味・品詞・種類などをまとめて確認できます。

**条件：** なし

**返ってくる情報：**  
全単語の一覧（単語・説明・品詞・種類）

---

## ユーザー関連

### 5. ユーザー登録

**URL：** `POST /api/userRegister`

**何ができるか：**  
新しいユーザーアカウントを作成します。  
登録が完了すると、そのままアプリを使い始めるためのログイン情報が返されます。

**送る情報：**

| 項目名 | 説明 |
|--------|------|
| last_name | 姓（例：山田） |
| first_name | 名（例：太郎） |
| email | メールアドレス |
| password | パスワード |

**返ってくる情報：**  
ユーザーIDと、ログイン状態を保持するための認証情報（トークン）

---

### 6. ログイン

**URL：** `POST /api/userLogin`

**何ができるか：**  
登録済みのメールアドレスとパスワードでログインします。  
ログインに成功すると、アプリを使い続けるための認証情報が返されます。

**送る情報：**

| 項目名 | 説明 |
|--------|------|
| email | 登録済みのメールアドレス |
| password | パスワード |

**返ってくる情報：**  
ユーザーIDと、ログイン状態を保持するための認証情報（トークン）

---

### 7. ログアウト

**URL：** `POST /api/userLogout`

**何ができるか：**  
現在ログイン中のユーザーをログアウトします。  
ログアウトした日時が記録されます。

**送る情報：**

| 項目名 | 説明 |
|--------|------|
| userId | ログアウトするユーザーのID |

**返ってくる情報：**  
処理の完了通知

---

## タイピング結果関連

### 8. タイピング練習の結果を保存する

**URL：** `POST /api/postTypeInfo`

**何ができるか：**  
タイピング練習を終えた後、その結果（正しく打てた回数・ミスした回数）を保存します。  
単語ごとの結果と、アルファベットごとの結果の両方を保存できます。

**送る情報：**

| 項目名 | 説明 |
|--------|------|
| userId | ユーザーのID |
| typWordInfo | 単語ごとのタイピング結果（単語名・成功回数・失敗回数） |
| typAlphaInfo | アルファベットごとのタイピング結果（文字・成功回数・失敗回数） |

**返ってくる情報：**  
処理の完了通知

---

### 9. 全単語ごとのタイピング回数を取得する

**URL：** `GET /api/typCountInAllWord`

**何ができるか：**  
指定したユーザーの、全単語に対するタイピングの成功回数・失敗回数を取得します。  
「どの単語が得意か・苦手か」を確認するのに使われます。

**指定する情報：**

| 条件名 | 説明 |
|--------|------|
| userId | ユーザーのID |

**返ってくる情報：**  
全単語ごとの成功回数・失敗回数の一覧（単語・品詞・種類・成功回数・失敗回数）

---

### 10. マイページのデータを取得する

**URL：** `GET /api/myPage`

**何ができるか：**  
ユーザーのマイページに表示するための統計情報をまとめて取得します。  
練習の合計回数やランキングなど、自分の成績を一覧で確認できます。

**指定する情報：**

| 条件名 | 説明 |
|--------|------|
| userId | ユーザーのID |

**返ってくる情報：**  

| 情報の種類 | 説明 |
|-----------|------|
| 単語タイピングの合計 | 単語全体での成功回数・失敗回数の合計 |
| アルファベットタイピングの合計 | アルファベット全体での成功回数・失敗回数の合計 |
| 単語タイピング回数ランキング | 最も多く打った単語のランキング（上位表示） |
| 単語ミス回数ランキング | 最もミスが多かった単語のランキング（苦手単語） |
| アルファベットタイピング回数ランキング | 最も多く打ったアルファベットのランキング |
| アルファベットミス回数ランキング | 最もミスが多かったアルファベットのランキング（苦手文字） |
